<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta content="width=device-width, initial-scale=1" name="viewport"/>
        <link rel="icon" type="img/png" href="./Images/Man_Yellow.png">
        <title>Gait Stroller</title>
        <link rel="stylesheet" href="./style2.css">
    </head>
    <body>
        <% if(success === false){ %>
            <h2 class="error"><%=message%></h2>
        <% } %>
        <h1>gaitSTROLLER</h1>

        <div class="centerImage">
            <img src="./Images/stroller1.jpg" alt="Gait Stroller">

            <% if(success === true){ %>
                <p class="success"><%=message%></p>
            <% }else{ %>
                <form action="/phaseone" method="post">
                    <h2>Leave your name and email so we can keep you in the loop as we bring gaitStroller to market!</h2>

                    <label>Name:
                        <input type="text" name="name" required>
                    </label>

                    <label>Email:
                        <input type="email" name="email" required>
                    </label>

                    <label>Zip Code:
                        <input type="text" name="zipCode">
                    </label>

                    <input type="hidden" name="page" value="<%=page%>">

                    <input class="button" class="button" type="submit" value="SUBMIT">
                </form>
            <% } %>

            <p class="subtitle">A CATEGORY REVOLUTION</p>
        </div>

        <div class="bottom">
            <div class="valueProp">
                <h3>An all-in-one child-carrying stroller designed for runners, fitness walkers and bicycle commuters</h3>

                <p>With exceptional engineering, the gaitStroller competes against high-end jogging strollers and bike trailers. But the gaitStroller brings more. With pivoting and telescoping rails, the gaitStroller has a novel hybrid function. gaitStroller can be pulled, on 2 wheels, from a runner’s waist. This transforms the running experience as it enables a natural running stride.</p>
            </div>
        </div>
    </body>
</html>